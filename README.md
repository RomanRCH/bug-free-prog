Random 6-digit Code Generator

A simple Python script that generates random numeric codes of specified length (default 6 digits).
Useful for verification codes, OTP, testing, etc.

Usage:
    python random_code.py

Features:
- Generates single or multiple unique codes
- Customizable length
